[workspace]
members = ["resx"]
default-members = ["resx"]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
debug = 1
incremental = false
overflow-checks = true
